# 自分メモ用

## 実装メモ

### TaskFormPage.tsx

useParams()
URLから date と id を取得する
→date = "2026-06-28"、id = "abc-123"

editingTask
id があれば既存タスクを探す
→ あれば編集モード・なければ作成モード

crypto.randomUUID()
ユニークなIDを自動生成する

## Github

### GithubへPushする手順

- git add .
- git commit -m "~"
- git push

## 用語

`タプル`とは 個数も順番も型も決まっている
